# Feature Matrix — Studio 3T / Querying

**Last reviewed:** 2026-07-31 — see [research findings](../../../../research/studio-3t-desktop-review-2026/02-querying-findings.md)

## Navigation

- [Product report](../../product-report.md)
- [Feature report](feature-report.md)
- [Feature dictionary](../../../../../feature-dictionary.md)
- [Shell feature](../shell/feature-matrix.md) — IntelliShell capabilities (SHELL-*)
- [Low-level comparison](../../../../../reports/comparisons/low-level-feature-comparison.md)

## Source index

- S1: https://studio3t.com/knowledge-base/articles/query-mongodb/
- S2: https://studio3t.com/knowledge-base/articles/table-view/
- S3: https://studio3t.com/knowledge-base/articles/collections-tab/
- S4: https://studio3t.com/knowledge-base/articles/date-tags/
- S5: Source-code audit, 2026-07 (see [research findings](../../../../research/studio-3t-desktop-review-2026/02-querying-findings.md)); citations reference `data-man-mongodb-ent/src/main/java/t3/` under the Studio 3T Desktop source checkout.

## Capability matrix

| Capability ID | Capability | Current support | Detailed behavior | Constraints / prerequisites | Roadmap status | Sources | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| QUERY-filter-bar | Collection tab query bar | Supported | Five fields: Query, Projection, Sort, Skip, Limit. Auto-completion for {, [, " pairs; field names; operators; BSON type wrappers; Ctrl+Space trigger. Syntax coloring. Shorthand expansions: bare ObjectId hex → ObjectId("...") query, bare UUID, bare number, bare quoted string. Query JSON Editor with Validate button. Run variants: find(), findOne(), count(). | Query must be valid JSON (with BSON extensions). | partial — edition claim corrected | S1, S3, S5 | The query bar itself is not separately gated, but the attached query-code-generation toolbar and the underlying `COLLECTION_TAB` app feature are checked via `App.hasFeatureOrIsNotFreeAndRobo(AppFeatures.COLLECTION_TAB)` (`OpenConnectionsTree.java:4436,4458,4501,4517`, `ShellMenu.java:1681`, `AppWindow.java:7086`); Free and Community(Robo) editions are excluded unless the specific sub-feature is in their `Edition` set (`Edition.java:96-120`). "All editions" is not accurate without qualification — exact sub-part gating needs further re-verification. |
| QUERY-history | Query history and save/load | Supported | Queries auto-saved every run to history. Manual save to local files or shared folders. Saved queries include connection + database + collection target details. Default query per collection (auto-runs on open). | Shared folder save requires Pro/Base+. Not available in Free edition (see notes). | partial — edition claim corrected | S1, S5 | Corrected: `AppFeatures.SAVED_QUERIES_AND_HISTORY` is present in `ROBO` (Community, `Edition.java:107`) but **absent from `FREE`** (`Edition.java:109-120`). Gated via `App.hasFeatureOrIsNotFreeAndRobo` (`utils/App.java:254-256`) throughout `bookmarkmanager/BookmarkWithOverviewPane.java`, `LoadQueryToolItem.java`, `intellishell/IntelliShellEditor.java`, `screens/aggregate/AggregationComposite.java`, `sqltab/SqlTab.java`. Net effect: Free edition does **not** get saved queries/history; Community (Robo) and all paid tiers do. "History: all editions" was incorrect. Shared folder save remains Pro/Base+ only. |
| QUERY-open-in | Open query in other tools | Supported | "Open in IntelliShell" sends current query. "Open in Aggregation Editor" converts find() filters and projections into equivalent pipeline stages ($match, $project). A third target, "Open in Collection Tab", also exists. | Conversion to aggregation stages is best-effort; complex operators may need manual adjustment. | confirmed | S1, S5 | `QueryCodeWidget.java` implements `createIntelliShellToolItem` (line 279), `createAggregateEditorToolItem` (line 296), and `createCollectionTabToolItem` (line 313) — the third open-in target was not previously called out. Availability follows the same edition gating as the query bar/code-generation toolbar. |
| QUERY-view-table | Table View | Supported | Rows/columns result view. In-place editing via double-click. Sort by column headers. Column reorder via drag. Hide/show columns (Ctrl+T). Auto-size compact or full. Expand embedded fields with breadcrumb trail navigation. Step into arrays at column or cell level. Copy as CSV. Copy for Excel. BSON type coloring (configurable). Restore default view. | Very wide documents or deeply nested structures may require manual column configuration. | unverified (not re-checked this pass) | S2 | Not re-verified in the 2026-07 source-code audit — the investigated directories (`querycode`, `search`, `history`, `bookmarkmanager`, `queryprofiler`, `querybuilder`) do not include the result-view/`CollectionViewer` code. No contradicting evidence found; "All editions" claim left as previously documented but should be treated as unconfirmed pending a follow-up pass targeting `t3/utils/collectionview/`. |
| QUERY-view-tree | Tree View | Supported | Hierarchical document display. In-place editing. Multi-line editor (ellipsis button) for long values. | — | unverified (not re-checked this pass) | S1 | Same caveat as QUERY-view-table — not independently re-verified in this audit. |
| QUERY-view-json | JSON View and document editor | Supported | Scrollable JSON documents per result. Document JSON Editor (Ctrl+J) for full-document editing. Validate JSON before save. | — | unverified (not re-checked this pass) | S1 | Same caveat as QUERY-view-table — not independently re-verified in this audit. |
| QUERY-multi-update | In-place multi-document update | Supported | "Update Documents" dialog: equivalent to updateMany(); separate query tab and update tab; Collection History recording option; Validate JSON. | Requires write access to the collection. | unverified (not re-checked this pass) | S1 | Same caveat as QUERY-view-table — not independently re-verified in this audit. |
| QUERY-vqb-core | Visual Query Builder (VQB) | Supported | Drag-and-drop cells/fields from Table View (single) or Tree View (multi-select via Ctrl). VQB and Query Bar bidirectionally sync in real time. Standalone or alongside IntelliShell. Top-level combiners: Match all ($and), Match any ($or), Match none ($nor). Nested AND/OR groups. Per-field operator dropdown + value field with auto-detected BSON type (manually overridable). Multiline text editor for long strings. Advanced editors for Binary, Reference, Regex. Clone field rules (duplicate button). | Array querying requires stepping into array column via "Has array element(s) matching" option. Not available in Free or Community (Robo) editions (see notes). | partial — edition claim corrected | S1, S5 | Corrected: `AppFeatures.VISUAL_QUERY_BUILDER` is checked directly via `App.hasFeature(...)` (not the Free/Robo-forgiving helper) in `dataman/mongodb/gui/querybuilder/QueryBuilder.java:1007,1019,1031`. `VISUAL_QUERY_BUILDER` is only in the `PROFESSIONAL` feature set (`Edition.java:51`), inherited by `CORE`/`PRO`/`ENTERPRISE` — it is **not** in `ROBO` or `FREE` (`Edition.java:96-120`). VQB is unavailable in both Free and Community editions; "All editions" was incorrect. Consistent with `QUERY-vqb-plan`. |
| QUERY-vqb-proj-sort | VQB Projection and Sort sections | Supported | VQB has distinct Query, Projection, and Sort sections. Code generation (Query Code tab) updates in real time as VQB is edited. | Same plan gating as QUERY-vqb-core (shares `AppFeatures.VISUAL_QUERY_BUILDER`) — not available in Free or Community (Robo) editions. | partial — edition claim corrected | S1, S5 | `dataman/mongodb/gui/querybuilder/ProjectionContainer.java`, `SortContainer.java` reference the same `VISUAL_QUERY_BUILDER` feature as QUERY-vqb-core. "All editions" was incorrect. |
| QUERY-date-tags | Date Tags | Supported | Tags available in: Collection Tab query bar, IntelliShell, VQB, Aggregation Editor $match stages, Export queries. Absolute: #today, #yesterday, #tomorrow. Hour: #lasthour, #nexthour, #lastNhours, #nextNhours. Days/weeks/months/years: #lastNdays, #lastcalweek, #thismonth, #thisyear, etc. Open-ended: #uptonow, #fromnow, #beforetoday, #aftertoday. Each expands to {$gt: <start>, $lt: <end>} at runtime. | N must be a positive integer; tags are Studio 3T–specific and not valid native MongoDB syntax. | confirmed, actively maintained | S4, S5 | All editions. Recent fixes: 2026.11.0 fixed date-hashtag handling when reused from Export→Aggregation Editor; 2025.4.0 fixed VQB active field losing focus when using date tags (`KONG-9917`/`KONG-9850`). |
| QUERY-manager | Query Manager | Supported | Organizes queries in folders (drag-and-drop). Filter by folder or search by name. Query preview panel. Create, edit (Quick edit), duplicate, delete. Supported query types: Collection query, IntelliShell script, SQL Query, Aggregation query. Auto-saved query history (separate store from saved queries). | SQL Query type requires Pro/Base+. Collection/IntelliShell/aggregation query types not available in Free edition (see notes). | partial — edition claim corrected | S1, S5 | Corrected: the same `SAVED_QUERIES_AND_HISTORY` gating documented under QUERY-history applies to the Query Manager's collection/IntelliShell/aggregation query types, not just the SQL type — Free edition lacks the feature entirely (Community/Robo has it). "All editions for collection/IntelliShell/aggregation" should read "Community Edition and paid tiers; not Free". SQL type remains Pro/Base+ only. |
| QUERY-export-lang | Export to language | Supported | Code-generation widget (Query Code tab) attached to the query bar. Exactly 9 generators registered: MongoDB Shell, JavaScript (Node.js), Java (2.x driver API), Java (3.x driver API), Java (4.x driver API), C#, Python, PHP, Ruby. | Each non-MongoShell language is a separately licensed `AppFeatures.QUERY_CODE_GENERATION_*` entry, available from `PROFESSIONAL` tier up only — not in Free or Community (Robo). | confirmed, language list corrected to precise names | S1, S5 | `t3/utils/codegenerators/ProgramCodeGenerators.java:31-41` registers exactly 9 generators (name confirmed per-generator, e.g. `javascript/JavaScriptProgramCodeGenerator.java:29` returns `"JavaScript (Node.js)"`). Previously documented loosely as "Node, Java, Python, etc." — corrected to the exact list. `AppFeatures.java:22-30`; `Edition.java:86-94`. |
| QUERY-view-gridfs | GridFS viewer | Supported | Browse, preview, upload/download GridFS-stored files; edit file/metadata. | Professional tier and up (`GRIDFS_TAB`, `GRIDFS_BUCKET_MANAGEMENT`). | confirmed | S5 | Added to the dictionary 2026-07-28 with no prior matrix row. `t3/dataman/gui/gridfsview/` contains `GridFsViewTab.java`, `GridFsFileNameEditor.java`, `GridFsMetaDataEditor.java`, `GridFsViewerComparator.java`. `AppFeatures.GRIDFS_TAB` and `AppFeatures.GRIDFS_BUCKET_MANAGEMENT` are both present only in `PROFESSIONAL` (`Edition.java:55,65`). |
| QUERY-view-split | Split panel views | Not confirmed | Dictionary describes multi-document/multi-panel side-by-side comparison with synchronized scrolling. | — | **absent / unverified — no source evidence found** | S5 | Exhaustive `grep -rl "SplitPanel"` and `grep -rli "sync.*scroll\|side.by.side"` across the full `data-man-mongodb-ent/src/main/java` tree returned zero matches. No split-panel/synchronized-scrolling document-comparison UI was found anywhere in the querying-adjacent code. This sub-feature was added to the dictionary on 2026-07-28 from studio3t.com marketing copy, not a source-code check. Marked absent/unverified for Studio 3T Desktop pending a recheck of whether it exists under an unexamined directory. |
| QUERY-charts-dashboards | Charts & dashboards | Not confirmed | Dictionary describes building charts from collection/query/pipeline data and combining into saved dashboards. | — | **absent / unverified — no source evidence found** | S5 | `grep -rli "dashboard"` across the whole module returned zero matches. The only chart-related code found is `dataman/gui/statsview/ServerStatusChartsTab.java` (server status metrics, an F-IDX concern) and AI Helper markdown chart rendering under `dataman/gui/chatGPT/response/rendering/` (response formatting, not a query-result chart/dashboard builder). This sub-feature was added to the dictionary on 2026-07-28 from studio3t.com marketing copy, not a source-code check. Marked absent/unverified for Studio 3T Desktop unless a different module was missed. |
| QUERY-value-search | Cross-collection value search ("Search in…") | Supported — **PENDING DICTIONARY ADDITION** | Searches a chosen set of collections in a database for a literal value and/or field name, with case-sensitive and regex toggles. Three search modes: Field value and name (`VALUE_NAME`), Field value (`VALUE`), Field name (`NAME`). | Professional tier and up (`AppFeatures.VALUE_SEARCH`). | confirmed (undocumented capability) | S5 | Not currently in the feature dictionary — proposed new ID `QUERY-value-search`. `t3/dataman/gui/search/SearchTab.java` (1517 lines) and `t3/utils/ValueSearcher.java` (`search(..., boolean caseSensitive, boolean regex, ..., SearchType searchType)`, lines 31-39; `SearchType` modes at lines 246-260). Gated by `AppFeatures.VALUE_SEARCH`, present from `PROFESSIONAL` tier up (`Edition.java:59`). Changelog: field-name search added 2022.8.0 ("Enhanced Value Search"); diacritics case-sensitivity fix 2024.5.0. Distinct from QUERY-filter-bar/VQB — searches *across* collections rather than filtering one. Requires this ID to be formally added to `feature-dictionary.md` before the "PENDING" marker can be removed. |
