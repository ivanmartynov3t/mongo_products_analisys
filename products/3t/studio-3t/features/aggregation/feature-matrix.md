# Feature Matrix — Studio 3T / Aggregation

**Last reviewed:** 2026-07-31 — see [research findings](../../../../../research/studio-3t-desktop-review-2026/03-aggregation-findings.md)

## Navigation

- [Product report](../../product-report.md)
- [Feature report](feature-report.md)
- [Feature dictionary](../../../../../feature-dictionary.md)
- [Low-level comparison](../../../../../reports/comparisons/low-level-feature-comparison.md)

## Source index

- S1: https://studio3t.com/knowledge-base/articles/query-mongodb/
- S2: [03-aggregation-findings.md](../../../../../research/studio-3t-desktop-review-2026/03-aggregation-findings.md) — 2026-07-31 source-code audit findings
- S3: Audited source tree, `t3/dataman/gui/screens/aggregate/` and `t3/utils/` (file/line citations per row), per S2
- S4: `release/studio-3t/changelog.txt`, per S2
- S5: git history, `t3/dataman/gui/screens/aggregate` and related paths (commit hashes cited per row), per S2

## Capability matrix

| Capability ID | Capability | Current support | Detailed behavior | Constraints / prerequisites | Roadmap status | Sources | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AGG-editor-layout | Pipeline editor layout | Supported | Multi-panel workspace: Pipeline panel (stage list with enable/disable checkboxes), Stage editor (raw-JSON editor for the selected stage), Stage input/output panels (per-stage Run, Count Documents), Pipeline output tab, and Query Code / Explain tabs. Per-file class evidence: `AggregationComposite.java`, `AggregationStageTab.java`, `AggregationStageResults.java`, `PipelineOutput.java`. | — | confirmed (layout present); **not re-verified in 2026-07-31 pass** | S2, S3, S4 | All editions. Changelog documents a 2026 UI redesign that replaced per-stage tabs with a list view — the historical "five-region" panel description should be visually re-verified against a current build rather than carried forward unchanged. |
| AGG-stage-mgmt | Stage management | Supported | Add stage (Shift+Ctrl+N), Add before/after selected stage, Move up (Shift+F8), Move down (F8), Enable/disable via checkbox or right-click, Run entire pipeline (F5), Run selection, Refresh (Ctrl+R). **Duplicate and Delete are right-click-context-menu only** (`duplicateStage()` / `deleteStage()`, `AggregationComposite.java:2851,2878`, reachable via `AggregationStageEditor.java:849-855`) — no registered keybinding exists for either in `ShortcutAction.java`. | — | confirmed, with a correction: no keyboard shortcut for Duplicate/Delete | S2, S3 | All editions. See AGG-keyboard for the full registered-shortcut list. |
| AGG-stage-toggle | Enable / disable individual stages | Supported | Each stage has a checkbox; disabled stages are skipped in pipeline execution without deletion. Also accessible via right-click context menu. | — | confirmed | S2, S3 | All editions. |
| AGG-pipeline-opts | Pipeline options | Supported | allowDiskUse toggle (`AggregationOptionsDialog.java:71-76`); custom collation (`CollationOptionsComposite`, dialog lines 97-105, requires server ≥3.4); index hint (`IndexHintComposite`, dialog lines 107-135, requires server ≥3.6). **maxTimeMS**: supported only when present in an imported/pasted pipeline options document, via the parsing path `MongoUtils.buildAggregationOptionsFromDocument():531` — there is no dedicated UI control to set it, and it is not written by the live-execution path `AggregationComposite.buildAggregationOptions()` (lines 3074-3091), which sets only collation and allowDiskUse. | allowDiskUse requires sufficient disk space on MongoDB server. Collation must match server locale support. Index hint and collation gated on MongoDB server version. maxTimeMS has no in-app Options-dialog control. | confirmed (allowDiskUse/collation/index hint); maxTimeMS confirmed as import-path-only, not a live Options UI field | S2, S3 | All editions. |
| AGG-code-gen | Code generation — multi-language | Supported | Query Code tab generates 9 registered generator entries (`ProgramCodeGenerators.java`) covering 7 language targets: MongoDB Shell, JavaScript (Node.js), Java (2.x/3.x/4.x driver API), C#, Python, PHP, Ruby. **Java driver versions are dropdown-selectable entries in a single language combo (`comboLanguage`, `QueryCodeWidget.java`), not separate sub-tabs.** "Open in IntelliShell" button for MongoDB Shell target. | Generated code reflects current pipeline state; manual review recommended before production use. | confirmed, with a correction: dropdown, not sub-tabs | S2, S3 | All editions. |
| AGG-export-results | Export from pipeline | Supported | Export Wizard can be launched from Pipeline output or from individual Stage I/O panels via `AggregationComposite.exportDocuments()` (~line 1129), delegating to `IExportable.exportDocuments()`. | Export target format and options follow the same Export Wizard configuration as standard exports. | confirmed | S2, S3 | All editions. |
| AGG-save-load | Save and load pipelines | Supported | Save as Studio 3T query (with optional saved target: connection + database + collection), as a .js file, or (new, 2026-05-12) **to a remote 3T Lens server** — `AggregationSaveMenuHandler.java` wires save-as-query, save-to-file, save-as-file, save-to-remote-server, and create-view. Load from Query Manager or .js file. | .js save omits target details; Studio 3T query format stores full context. 3T Lens save destination is a governance/3T Lens integration touchpoint — see notes. | confirmed | S2, S3, S5 | All editions. 3T Lens destination added in commit `f5c3fc7b562` (KONG-10915, 2026-05-12); coordinate with F-GOV's 3T Lens coverage. |
| AGG-create-view | Create view from pipeline | Supported | "Create view from aggregate query" option (`AggregationComposite.createView()`, line 2509; menu wiring `addCreateViewMenuItem()`, line 1656) generates a db.createView() call using the current pipeline; menu text toggles between create/save/update states. | Requires MongoDB ≥ 3.4 for view support. Requires write access to the database. | confirmed | S2, S3 | All editions. |
| AGG-vqb-sync | One-way handoff from Visual Query Builder | Supported | An "Open in aggregation editor" toolbar/menu action converts the current VQB/find query into a **new, independent** Aggregation Editor tab. This is a **one-way, on-demand conversion**, not a live sync: zero references to VQB/VisualQueryBuilder exist inside the aggregate package (`t3/dataman/gui/screens/aggregate/`); VQB code lives entirely in `t3/dataman/gui/CollectionTab.java` and `t3/dataman/mongodb/gui/querybuilder/`. | No live bidirectional syncing of edits between an open VQB panel and an open Aggregation Editor tab. | confirmed, with a correction: one-way handoff, not bidirectional sync | S2, S3, S4 | All editions. Previously documented as "bidirectional... side-by-side syncing" — that claim is not supported by source. |
| AGG-change-target | Change source collection / database / connection | Supported | Toolbar allows switching the target collection, database, or connection mid-session without losing the current pipeline. | The pipeline must be re-run after switching target. Field names in stages may no longer match if schema differs. | **not re-verified in 2026-07-31 pass** | S1, S2 | All editions. Toolbar target-switching UI is present in `AggregationComposite.java` by class structure, but the exact method was not isolated in the 2026-07-31 source pass — flagged as unverified rather than re-confirmed. |
| AGG-date-tags | Date Tags in $match stages | Supported | Date Tag ("hashtag") syntax (#today, #lastNdays, #lastcalmonth, etc.) usable in $match stage conditions; expands to {$gt, $lt} date range at execution time via `HashtagHelper.applyHashtagsToJson()` (`AggregationComposite.java:3023`, `AggregationParser.java:721`). Actively maintained: KONG-11042 (2026-06-04) fixed non-hashtag stages serialized as strings when reusing an aggregation query. | Date Tags are Studio 3T–specific; pipeline exported as .js will include the expanded literal values at save time. | confirmed, actively maintained | S1, S2, S3, S5 | All editions. |
| AGG-clipboard | Copy / paste entire aggregate query | Supported | Full pipeline JSON can be copied to clipboard (`AggregationComposite.java:1424,1435`) and pasted into another Aggregation Editor session or IntelliShell, routed through `updateQueryFromClipboardOrAIHelper()` (the same entry point also feeds the AI Helper — see F-AI cross-reference in the feature report). | — | confirmed | S2, S3 | All editions. |
| AGG-stage-preview | Per-stage input/output preview | Supported | `AggregationStageResults.java` implements a split Input/Output view per stage (`ExpandState.Split` / `InputCollapsed` / `OutputCollapsed`), backed by `PipelineOutput.java` / `ResultsSection.java`. | — | confirmed | S2, S3 | Promoted from dictionary-only; all editions. |
| AGG-pagination | Pipeline output pagination | Supported | `ResultsSection.java` (`lastPageSize`, `initPageSize`, `getCurrPageSize()`) and `AggregationTabCursorView.java` implement page-based navigation for pipeline output. | — | confirmed | S2, S3 | Promoted from dictionary-only; all editions. |
| AGG-timer-cancel | Live execution timer with cancel | Supported | `ExecutionPlaceholder.startTimeElapsedCount(LocalDateTime startTime)` (line 49) drives a live elapsed-time counter; a `cancelCallback` is threaded through `ResultsSection.showExecutionPlaceholder()`, `PipelineOutput`, `AggregationStageResults`, `AggregationStageTab`. | — | confirmed | S2, S3 | Promoted from dictionary-only; all editions. |
| AGG-keyboard | Keyboard shortcuts | Partially supported | `ShortcutAction.java:183-192` registers: `AGGREGATION_RUN_PIPELINE`, `AGGREGATION_SHOW_INPUT_TO_STAGE` (+ variant), `AGGREGATION_SHOW_OUTPUT_FROM_STAGE` (+ variant), `AGGREGATION_ADD_NEW_STAGE`, `AGGREGATION_MOVE_STAGE_UP`, `AGGREGATION_MOVE_STAGE_DOWN`. **No registered shortcut exists for Duplicate stage or Delete stage** — both are right-click-context-menu only. | — | partial — duplicate/delete have no keybinding | S2, S3 | Promoted from dictionary-only; all editions. See AGG-stage-mgmt. |
| AGG-js-import-export | JS file import/export | Supported | Save/load routes through `ScriptUtils.selectAndOpenScript()` → `FileContentType.AGGREGATE_QUERY_SCRIPT` (`t3/utils/FileContentType.java:40-44`) using `FileType.JAVASCRIPT` (`t3/utils/FileType.java:12`), confirming mongosh-format `.js` import/export distinct from the Studio 3T query save path. | — | confirmed | S2, S3 | Promoted from dictionary-only; all editions. |
| AGG-code-tab | Query code tab | Supported | `QueryCodeWidget` embedded as `resultQueryCodeWidget` (`AggregationComposite.java:253,2126`); the MongoDB Shell generator entry provides the bidirectional JSON/mongosh representation of the pipeline. | — | confirmed | S2, S3 | Promoted from dictionary-only; all editions. |
| AGG-stage-count | Stage catalog | Supported | `AggregationPipelineStage.java` enum lists **36 named stages** (`$addFields` through `$unwind`) plus a generic `OTHER` fallback (line 1070). Confirmed absent: `$searchMeta` and `$vectorSearch` (zero hits repo-wide as of 2026-07-31). | Absence of `$searchMeta`/`$vectorSearch` is a point-in-time finding — should be re-checked on the next audit pass since these map to active MongoDB/Atlas feature areas. | confirmed, with exact count | S2, S3 | Promoted from dictionary-only; all editions. |
| AGG-stage-modes | Stage edit modes | Partially supported (corrected) | Each stage has **one** raw-JSON editor (`AggregationStageEditor.stageEditor`, an `Editor`/`SourceViewer` over a `StyledText`, lines 95, 326-337). Selecting an operator from `stageOperatorCombo` inserts a pre-built JSON template (`AggregationPipelineStage.getOperatorSpecificationTemplates()`) into that same raw editor. **There is no separate form/visual mode and no wizard mode anywhere in the package.** | — | partial — corrected from a prior "form/visual + raw JSON + wizard" description; raw JSON only, with operator-picker template insertion | S2, S3 | Corrects a dictionary-only description; all editions. |
| AGG-chart-builder | Chart / visualization builder from pipeline output | Not supported | Zero "chart" hits anywhere in `t3/dataman/gui/screens/aggregate/`. A JavaFX chart component (`FxChartBuilder.java` and related classes) exists elsewhere in the codebase but is tied to Server Status Charts and Schema Explorer value charts, not aggregation pipeline output. | — | absent (zero-hit grep, 2026-07-31) — re-check on next audit pass | S2, S3 | Promoted from dictionary-only as an absence finding, not a roadmap claim; all editions. |
