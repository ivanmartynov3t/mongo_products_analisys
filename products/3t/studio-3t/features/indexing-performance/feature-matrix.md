# Feature Matrix — Studio 3T / Indexing & Performance

## Navigation

- [Product report](../../product-report.md)
- [Feature report](feature-report.md)
- [Feature dictionary](../../../../../feature-dictionary.md)
- [Low-level comparison](../../../../../reports/comparisons/low-level-feature-comparison.md)

**Last reviewed:** 2026-07-31 — see [research findings](../../../../../research/studio-3t-desktop-review-2026/05-indexing-performance-findings.md)

## Source index

- S1: https://studio3t.com/knowledge-base/articles/mongodb-index-strategy/
- S2: https://studio3t.com/knowledge-base/articles/mongodb-explain-visualized/
- S3: https://studio3t.com/knowledge-base/articles/mongodb-query-performance/

## Capability matrix

| Capability ID | Capability | Current support | Detailed behavior | Constraints / prerequisites | Roadmap status | Sources | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| IDX-inventory | Index Manager display | Supported | Lists all indexes for a collection: name (expandable to fields + sort order), type, size, usage count. Total index size displayed in bottom-right. Usage count = times used since index creation or last server restart. Side-by-side index view across databases. | Usage display requires the $indexStats privilege on the MongoDB user. | confirmed | S1 | All editions. |
| IDX-type-single | Index type — Single field | Supported | Single field with sort order (1 ascending, -1 descending). Sort order is irrelevant for single-field queries but matters for range queries and sort operations. Studio 3T surfaces this context in the Add Index dialog. | — | confirmed | S1 | All editions. |
| IDX-type-compound | Index type — Compound | Supported | Multiple fields; Studio 3T recommends ESR (Equality, Sort, Range) field ordering. Sort order per field configurable. | ESR ordering is a recommendation, not enforcement. | confirmed | S1 | All editions. |
| IDX-type-multikey | Index type — Multikey | Supported | Array field: MongoDB automatically creates a key per array element. Compound multikey restrictions apply (at most one array field in a compound multikey index). | — | confirmed | S1 | All editions. |
| IDX-type-text | Index type — Text | Supported | Version 1/2/3 (default 3). Default language (English). Language override field. Per-field weights. One text index per collection enforced by MongoDB. | Only one text index per collection is a hard MongoDB constraint, not a Studio 3T limit. | confirmed | S1 | All editions. |
| IDX-type-wildcard | Index type — Wildcard | Supported | $** (all fields) or specific dotted path. Designed for collections with unknown or dynamic field names. | Wildcard indexes have performance trade-offs on large collections; Studio 3T does not warn about these. | confirmed | S1 | All editions. |
| IDX-type-2d | Index type — 2D | Supported | Flat-geometry 2d index; configurable lower/upper bound and bit precision (default 26 bits ≈ 60 cm resolution). | — | confirmed | S1 | All editions. |
| IDX-type-2dsphere | Index type — 2DSphere | Supported | Earth-sphere GeoJSON geospatial index. | — | confirmed | S1 | All editions. |
| IDX-type-haystack | Index type — geoHaystack | Supported with constraints | Bucket size configurable. Deprecated in MongoDB 4.4, removed in MongoDB 5.0. Studio 3T version-gates the option to servers before 4.9 and **does show an in-UI deprecation warning**: "As of MongoDB 4.4, Bucket Size/geoHaystack is deprecated." | Only offered on MongoDB < 4.9 (`util/FieldUtils.IndexType.GEOHAYSTACK` version gate). Creating a geoHaystack index on MongoDB ≥ 5.0 will fail. | confirmed | S1 | All editions. Deprecated in MongoDB 4.4; removed in 5.0. Corrected 2026-07-31: the matrix previously claimed Studio 3T does not warn about deprecation — it does, per `v2/IndexGeoOptionsSubTab.java:215`. |
| IDX-props-unique-sparse | Index properties — Unique & Sparse | Supported | Unique: rejects duplicate field values. Sparse: skips documents missing the indexed field. | — | confirmed | S1 | All editions. |
| IDX-props-hidden | Index properties — Hidden | Supported | Index excluded from query planner but constraints still enforced; requires MongoDB ≥ 4.4 and featureCompatibilityVersion ≥ 4.4. | Requires MongoDB ≥ 4.4 and featureCompatibilityVersion ≥ 4.4. | confirmed | S1 | All editions. |
| IDX-type-ttl | Index type — TTL | Supported | Single-field date-type index with automatic document expiry in seconds. | TTL requires a Date-type field. | confirmed | S1 | All editions. |
| IDX-props-partial | Index properties — Partial | Supported | Filter expression to index only matching documents. | Requires a valid filter expression. | confirmed | S1 | All editions. |
| IDX-props-collation | Index properties — Collation (case-insensitive) | Supported | Case-insensitive index via collation (locale required; strength 1 or 2). | Locale must be specified; case-insensitivity requires strength ≤ 2. | confirmed | S1 | All editions. |
| IDX-build-mode | Background vs foreground index creation | Supported | Unchecked = foreground (compact index, blocks reads + writes during build). Checked = background (non-blocking build, less compact, size converges over time). The "Create in background" checkbox is accompanied by in-UI explanatory text describing the MongoDB 4.2+ locking behavior change and the background-build size/compactness tradeoff. | Foreground creation is blocking — avoid on production with active traffic. Background index builds are superseded by concurrent builds in MongoDB 4.4+; Studio 3T explains the tradeoff and the 4.2+ locking change in-UI, though it does not use the literal word "deprecated." | confirmed | S1 | All editions. Corrected 2026-07-31: the matrix previously claimed Studio 3T does not flag this in the UI — it does surface an explanation, per `IndexConfigurationDialog.java`'s `createBackgroundButton` help text. |
| IDX-copy-paste | Index operations — Copy / Paste across collections | Supported | Copy index definition from one collection, paste to another collection, confirmed genuinely cross-connection (source and target take independent `Connection` objects, not just cross-collection on the same connection). Useful for replicating index strategies across environments. | Destination collection must exist and be writable. | confirmed | S1 | **Professional edition and above** — gated behind `AppFeatures.COPY_INDEX`, which is listed only in the `PROFESSIONAL` feature set (`utils/Edition.java`); absent from Free and Community (Robo) editions. Corrected 2026-07-31: the matrix previously claimed "All editions," which was incorrect. |
| IDX-hide-unhide | Index operations — Hide / Unhide | Supported | Hide removes the index from query planner consideration without dropping it. Unhide restores it. Allows safe index impact testing. | Requires MongoDB ≥ 4.4 and featureCompatibilityVersion ≥ 4.4. | confirmed | S1 | All editions. |
| IDX-explain-brief | Visual Explain — Brief mode | Supported | Default mode: plan diagram only with no runtime statistics. Always fast and always available regardless of query complexity or collection size. Suitable for plan type verification (COLLSCAN vs IXSCAN). | Brief mode does not show execution time or document counts. | confirmed | S2 | All editions. |
| IDX-explain-full | Visual Explain — Full mode | Supported | Click "Run full explain" to add runtime statistics per stage: output document count, execution time (ms) per stage and total, estimated bytes processed per stage. Hover tooltips on arrows and stage blocks. Per-stage detail: right-click → "What's this?" for description; "View original JSON fragment" for raw JSON scoped to that stage. View full query plan JSON ("View JSON" button). | Full mode executes the query against the database; avoid on expensive queries in production. | confirmed | S2 | All editions. |
| IDX-explain-sources | Visual Explain integration points | Supported | Available in: Collection Tab, IntelliShell (Query Assist tabs), SQL Query tool, Aggregation Editor, and Query Profiler. | IntelliShell Visual Explain only available in Query Assist mode (not Shell mode). | confirmed | S2 | All editions. |
| IDX-profiler-config | Query Profiler | Supported | Reads from MongoDB's system.profile collection. Profiling levels: Level 0 (Off), Level 1 (slow queries > 100ms), Level 2 (all). Studio 3T toggles Level 0 ↔ Level 1. Query grouping: Exact query (unique groups by field names + values) or Query shape (groups by structure; shows queryHash column). Filters by collection and timeframe (e.g., last 30 days). | Requires MongoDB Database Profiler enabled; $indexStats privilege required for usage display. Level 2 profiling can significantly impact server performance. | confirmed | S3 | All editions. |
| IDX-profiler-drilldown | Query Profiler drill-down and integration | Supported | Per-execution detail: Duration, Timestamp, Keys Examined, planSummary. Actions: View Full JSON (system.profile document), Query Code tab (MongoDB Shell syntax), "Open in IntelliShell", "Open in Collection Tab" (find queries), "Open in Aggregation Editor" (aggregation), Explain plan tab (embedded Visual Explain). Right-click context → "Add Index on collection" shortcut. | "Open in Collection Tab" and "Open in Aggregation Editor" require the relevant query type. | confirmed | S3 | All editions. |
| IDX-type-hashed | Index type — Hashed | Supported | Hashed index type is selectable in the field-type combo, for equality lookups and as a hashed sharding-key algorithm. | Hashed indexes support equality only, not range queries or sorts. | confirmed | S1 | All editions. Added 2026-07-31 — previously not in matrix. Evidence: `util/FieldUtils.IndexType.HASHED("hashed","hashed")`; detected in `data/IndexInfo.getIndexType()`. |
| IDX-atlas-search | Atlas Search index management | Not supported | No Atlas Search (Lucene) index creation/management UI exists in Index Manager. `$search` appears only as a query/aggregation-authoring helper (query builder text-search input, an aggregation pipeline `SEARCH` stage template), not as index management. | — | absent | — | Not applicable. Confirmed absent 2026-07-31 — exhaustive grep across `indexmanager/` and the wider codebase for "search"/"lucene" found no Atlas Search index UI; not mentioned in the changelog. |
| IDX-vector-search | Vector Search index management | Not supported | No Vector Search index creation/management UI exists anywhere in the codebase. | — | absent | — | Not applicable. Confirmed absent 2026-07-31 — grep for "vectorSearch"/"knnVector"/"vector search" across `t3/dataman` returned zero hits; not mentioned in the changelog. |
| IDX-collation-opts | Collation index — full options | Supported | Full collation configuration well beyond simple case-insensitivity: ~150-locale picker, strength 1–5, case-first (upper/lower/off), alternate, max-variable, case level, numeric ordering, backwards diacritic sort, normalization. | Locale must be specified to enable collation. | confirmed | S1 | All editions. Added 2026-07-31 — previously not in matrix; see also IDX-props-collation, which understates this same capability. Evidence: `util/CollationUtils.java`, `v2/IndexCollationSubTab.java`. |
| IDX-text-opts | Text index — full options | Supported | Text index version (1/2/3, default 3), default language and language-override field (~15 languages), per-field weights (validated 1–99,999). | One text index per collection is a hard MongoDB constraint, not a Studio 3T limit. | confirmed | S1 | All editions. Added 2026-07-31 — previously not in matrix. Evidence: `util/TextOptionUtils.java`, `v2/IndexTextOptionsSubTab.java`, `edit/IndexWeightEditingSupport.java`, `edit/WeightFieldChooserDialog.java`. |
| IDX-geo-opts | Geo index — full options | Supported | 2d index: lower/upper bound and bit precision configuration. 2dsphere index: index version (1–3) configuration. | — | confirmed | S1 | All editions. Added 2026-07-31 — previously not in matrix. Evidence: `util/GeoOptionsUtils.java` (`OPTION_2D_MIN/MAX/BITS`, `OPTION_2D_SPHERE_INDEX_VERSION`), `data/IndexInfo.get2dMin/get2dMax/get2dBits/get2dSphereIndexVersion()`. |
| IDX-advanced-opts | Advanced index options | Partially supported | Partial filter expression: fully supported via a dedicated UI composite. Wildcard projection: **not exposed in the UI** — the driver option is referenced only in `equals()`/`hashCode()`, with no setter or UI field found. Commit quorum: **not found anywhere** in the codebase. Clustered index: detection/viewing only — Studio 3T recognizes an existing clustered index (treats it as non-droppable, labels the dialog title "(Clustered Index)") but has no UI to create one from Index Manager. | — | partial | S1 | All editions (for partial filter expression). Added 2026-07-31 — previously not in matrix. Evidence: `v2/PartialFilterExpressionComposite.java`; `data/IndexInfo.java`; `util/IndexManagerUtils.isClustered()`, `v2/IndexConfiguration.java`. |
| IDX-quick-actions | Quick-action index presets | Not supported | No one-click common-index-creation preset mechanism was found. | — | absent | — | Not applicable. Confirmed absent 2026-07-31 — grep for "preset"/"quick action" in `indexmanager` returned zero hits. |
| IDX-profiler-analysis | Query Profiler analysis | Supported (partial) | Two confirmed grouping modes: exact query (groups by unique field names + values) and query shape (groups by structure, shown via `queryHash`). "Percentile lines" and explicit "COLLSCAN flags" in the profiler UI were **not located** in the files reviewed and need follow-up verification before being treated as confirmed. | — | partial | S3 | All editions. Added 2026-07-31 — previously not in matrix. Evidence: `queryprofiler/QueryProfilerTab.java` (`isGroupByQueryHash`, `groupByQuery`/`groupByQueryHash`), `queryprofiler/util/QueryProfilerUtils`. |
| IDX-profiler-export | Query Profiler export | Not supported | No CSV/JSON export wiring exists in the Query Profiler package. A generic export framework exists elsewhere in the codebase (`t3.utils.export.*`) but the profiler does not use it. | — | absent | — | Not applicable. Confirmed absent 2026-07-31 — grep for "export"/"csv" in `queryprofiler/` returned zero hits; not mentioned in the changelog. |
| IDX-profiler-live | Query Profiler live monitoring | Not supported (as a profiler feature) | No live-monitoring/kill-operation UI exists inside the Query Profiler package specifically. The equivalent real-time capability lives under a different tool entirely — see IDX-realtime-perf / IDX-stop-ops (Current Operations). | — | absent | — | Not applicable to the Query Profiler. Confirmed 2026-07-31. See IDX-realtime-perf for the actual feature and its edition gating. |
| IDX-perf-insights | Performance insights / advisor | Not supported | No advisor/insight/recommendation feature for indexes or query performance exists. | — | absent | — | Not applicable. Confirmed absent 2026-07-31 — grep for "advisor"/"insight"/"recommendation" tied to indexes or performance returned no relevant hits in `t3/dataman`; not mentioned in the changelog. |
| IDX-realtime-perf | Real-time performance monitoring | Supported | Two independent, verified capabilities: (1) live server-status charts — a mongostat-equivalent that polls `serverStatus` on a timer and charts opcounters (query/getmore/insert/update/delete), connections (current/available/totalCreated), and network throughput (numRequests/bytesIn/bytesOut) as auto-refreshing time series; (2) a Current Operations view — a currentOp-equivalent listing in-flight server operations, reachable from the connection tree and database menu. No dedicated mongotop-style per-collection activity monitor was found. | Requires Professional edition or above. | confirmed | — | **Professional edition and above only** — gated behind `AppFeatures.SERVER_STATUS_CHARTS` and `AppFeatures.CURRENT_OPERATIONS`, both listed only in the `PROFESSIONAL` feature set (`utils/Edition.java`); absent from Free and Community (Robo) editions. Added 2026-07-31 — previously not in matrix; a notable differentiator worth calling out in the feature report. Evidence: `gui/statsview/ServerStatusChartsTab.java`, `gui/taskview/TaskMonitorTab.java`, `gui/ShellMenuDatabase.java`, `gui/OpenConnectionsTree.showCurrentOps()`. |
| IDX-stop-ops | Stop/kill long-running operations | Supported | Kill-operation action available directly from the Current Operations view. | Requires Professional edition or above (same gate as IDX-realtime-perf). | confirmed | — | Professional edition and above only. Added 2026-07-31 — previously not in matrix. Evidence: `gui/taskview/TaskMonitorTab.killOperation()`, `MongoDBConnector.killOperation()`. |
